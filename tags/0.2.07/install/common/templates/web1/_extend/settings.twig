class="settings-element {{ Options.HideBreadcrumb ? "nobreadcrumb" : "" }}"
     data-root_path="{{ user_info.include_path }}{{ Options.DataPath }}"
     data-id="{{ Options.Id }}"
     data-manager="{{ IsSearchPage ? Options.jsSearchObject : Options.jsManageObject }}"
     data-parent_id="{{ Options.ParentObjectId }}"
     data-parent_object="{{ Options.ParentObjectType }}"
